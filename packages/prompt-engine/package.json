{
	"name": "@njust-ai-cj/prompt-engine",
	"description": "Pure prompt rendering engine for NJUST_AI_CJ.",
	"version": "0.0.0",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"clean": "rimraf dist .turbo"
	},
	"devDependencies": {
		"@njust-ai-cj/config-typescript": "workspace:^",
		"@types/node": "20.x",
		"@vitest/coverage-v8": "^3.2.3",
		"vitest": "^3.2.3"
	}
}
