# Development overrides - automatically loaded by docker compose
# These settings only apply when running locally for development
#
# For production, use: docker compose -f docker-compose.yml up
# (explicitly exclude override file)

services:
    web:
        environment:
            - NODE_ENV=development
        volumes:
            # Mount log files so web can access task logs
            - /tmp/evals:/tmp/evals:ro
            # Mount source code for hot reload in development
            - ../../apps/web-evals:/njust-ai/repo/apps/web-evals:delegated
            - ../../packages/evals:/njust-ai/repo/packages/evals:delegated
            - ../../packages/types:/njust-ai/repo/packages/types:delegated
            - ../../packages/ipc:/njust-ai/repo/packages/ipc:delegated
            # Exclude node_modules from being overwritten
            - /njust-ai/repo/node_modules
            - /njust-ai/repo/apps/web-evals/node_modules
            - /njust-ai/repo/packages/evals/node_modules
            - /njust-ai/repo/packages/types/node_modules
            - /njust-ai/repo/packages/ipc/node_modules
        entrypoint: []
        command:
            - sh
            - -c
            - |
                echo '🚀 Starting evals web service in development mode...'
                wait_for_db() {
                    echo '⏳ Waiting for database...'
                    until pg_isready -h db -p 5432 -U postgres -d evals_development > /dev/null 2>&1; do
                        echo '⏳ Database not ready yet, waiting 2 seconds...'
                        sleep 2
                    done
                    echo '✅ Database is ready'
                }
                wait_for_db
                echo '🔄 Running database migrations...'
                pnpm --filter @njust-ai/evals db:migrate
                echo '🌐 Starting Next.js dev server...'
                cd /njust-ai/repo/apps/web-evals && npx next dev -p 3446
