/**
 * Mode string constants shared across the extension (no dependency on core).
 * `DEFAULT_MODE_SLUG` must stay aligned with `DEFAULT_MODES[0].slug` in `@njust-ai-cj/types`.
 */
export const DEFAULT_MODE_SLUG = "cloud-agent" as const
