{
	"name": "@njust-ai/vscode-nightly",
	"version": "0.0.1",
	"private": true,
	"description": "Nightly build for the NJUST_AI VSCode extension.",
	"scripts": {
		"bundle:nightly": "node esbuild.mjs",
		"vsix:nightly": "cd build && mkdirp ../../../bin && npx vsce package --no-dependencies --out ../../../bin",
		"clean": "rimraf build .turbo"
	},
	"devDependencies": {
		"@njust-ai/build": "workspace:^"
	},
	"packageManager": "pnpm@10.8.1"
}
